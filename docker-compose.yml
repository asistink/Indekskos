version: '3.8'

services:
  backend:
    build: .
    container_name: indekskos_backend
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
